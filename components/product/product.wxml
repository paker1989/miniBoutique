<view class='product-card' bind:tap="seeProductDetails">
  <view class="image-container">
    <image src="{{image}}" mode="aspectFill" />
  </view>
  <view class="goods-title">{{truncatedTitle}}</view>
  <view class="price-container">{{currency + ' ' + formattedPrice}}</view>
</view>